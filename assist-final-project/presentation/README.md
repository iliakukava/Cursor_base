# Презентация TeleFlow (статический сайт)

Папка: только **HTML + CSS + JS**, без сборки и npm. 12 слайдов, оптимизировано под питч 5–7 минут.

## Как открыть

- Дважды кликнуть по `index.html`, **или**
- Локальный сервер (нужен только для Google Fonts без CORS-warning в некоторых браузерах):

```bash
cd presentation
python -m http.server 8765
```

Открыть: `http://127.0.0.1:8765/`

## Управление

- **Прокрутка** колёсиком/тачпадом — секции прилипают по высоте экрана (`scroll-snap`).
- **Стрелки** <kbd>↑</kbd>/<kbd>↓</kbd>/<kbd>←</kbd>/<kbd>→</kbd>, <kbd>Page Up/Down</kbd>, <kbd>Space</kbd> — между слайдами.
- <kbd>Home</kbd>/<kbd>End</kbd> — в начало/конец.
- **Точки слева** (на мобильных — снизу) — переход к разделу.
- Жесты свайпа — переключение слайдов.
- <kbd>F</kbd> — полноэкранный режим.
- <kbd>P</kbd> — печать / экспорт в PDF (страницы 1440×900).

В правом верхнем углу — компактный счётчик текущего слайда (`NN / NN`).

## Структура слайдов

| #  | id             | Что                                                            |
|----|----------------|----------------------------------------------------------------|
| 01 | cover          | Заголовок, крючок                                              |
| 02 | problem        | Боль автора/эксперта в Telegram                                |
| 03 | product        | Четыре команды и контур данных                                 |
| 04 | flow           | SVG-диаграмма потока с пульсом активного узла                  |
| 05 | value          | Польза в цифрах (KPI-блоки)                                    |
| 06 | ai             | Две точки вызова LLM + параметры батчей и бюджетов             |
| 07 | reliability    | Whitelist, DRY_RUN, FloodWait, атомарная запись, прокси         |
| 08 | course-stack   | Карта 8 модулей курса → артефакты в проекте                    |
| 09 | time           | ≈ 40 часов, разбивка по блокам                                 |
| 10 | roadmap        | Три горизонта развития (автор → B2B → платформа)               |
| 11 | course         | Что забрал с курса и куда расту                                |
| 12 | cta            | Оффер команде АССИСТ+                                          |

## Что править под себя

- Цифры экономии на слайде 5 и разбивка времени на слайде 9 — замени под свои реальные замеры.
- Имя бренда TeleFlow — в `index.html` (слайды 1 и 12).
- Цвета, шрифты, радиусы — в `:root` в `style.css`.

## Печать в PDF

`Ctrl/Cmd+P` → Save as PDF, страницы 1440×900 настроены через `@media print` в `style.css`. Навигация, счётчик и прогресс-бар при печати скрыты.
